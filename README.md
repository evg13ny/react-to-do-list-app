Simple To-Do List made with React, React Hooks, React-Bootstrap, React Font Awesome.

For start:

* git clone [repository url]
* cd [local repository]
* delete package-lock.json
* npm install
* npm start

Keys:

You can add, edit, delete, filter tasks.